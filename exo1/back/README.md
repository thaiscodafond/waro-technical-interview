# Exo 1 - back

This run a simple express API.

## Installation

- Run `pnpm install`

## Running server

Server is running at http://localhost:4000

- Run `pnpm start`

## Routes

- `products` : returns a list of products
- `product/:id` : return a specific product
